spring:
  application:
    name: config

  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/chenshaojian/base-config-repo.git
          username:
          password:

  rabbitmq:
    addresses: amqp://localhost:5672
    username: alan
    password: alan